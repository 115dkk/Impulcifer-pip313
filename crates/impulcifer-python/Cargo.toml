[package]
name = "impulcifer-python"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
name = "impulcifer_native"
crate-type = ["cdylib", "rlib"]

[lints]
workspace = true

[dependencies]
impulcifer-types = { path = "../impulcifer-types" }
# The Python surface has no recording entry point, and manylinux forbids libasound.
impulcifer-service = { path = "../impulcifer-service", default-features = false }
impulcifer-cli = { path = "../impulcifer-cli", default-features = false }
serde_json.workspace = true
pyo3 = { version = "0.27", features = ["extension-module", "abi3-py39"], optional = true }

[features]
default = []
python = ["dep:pyo3"]
