# P11 (ASTRA): `impulcifer-service` BRIR orchestration — `start_brir` end to end on the existing webview_ui IPC

You are extending the Impulcifer 3.x Rust workspace at `E:/Impulcifer`. Read first, in this order: `E:/Impulcifer/docs/rust/ARCHITECTURE.md` sections 3.2, 3.3, 5 and 7; `E:/Impulcifer/docs/rust/survey/pipeline-object-model.md` (sections `core/pipeline_stages.py`, `core/pipeline.py`, "Files read", "Files written"); the 2.x oracle `E:/Impulcifer/application/impulcifer_service.py` (`_validate_brir_request`, `start_brir` lines 428 to 494, `_start_job`, `poll_job`, `_emit`, `_finish_job`), `E:/Impulcifer/infra/logger.py` (`step`, `progress`, `info`/`warning` with i18n keys, lines 55 to 145), `E:/Impulcifer/core/pipeline.py` (`run`, every `_stage_*` body: which logger key each stage emits and the order of file writes), `E:/Impulcifer/core/pipeline_stages.py` (`open_impulse_response_estimator` 85 to 180, `headphone_compensation` file resolution 369 to 418, `equalization` 294 to 350, `write_readme` 525 to 687), `E:/Impulcifer/core/brir_layout.py` (`append_track_names`, `read_track_names`, the `ICHL` chunk), `E:/Impulcifer/core/sweep_detection.py` (`detect_sweep_parameters`, `snap_sweep_samples`); then the Rust crates you build on: `crates/impulcifer-service/src/{lib.rs,args.rs,paths.rs,settings.rs}` (the dispatcher, argument helpers, i18n loading, settings), `crates/impulcifer-jobs/src/registry.rs` (`JobRegistry::start`, `JobContext::{progress,log,check_cancelled}`), `crates/impulcifer-dsp/src/{pipeline.rs, stages/, estimator.rs, hrir.rs, ir.rs, fr.rs}` (P08, P09, P10; use their types), `crates/impulcifer-io/src/{wav.rs, sweep_files.rs, ffmpeg.rs}`, and `crates/impulcifer-types/src/{config.rs, ipc.rs, stages.rs, constants.rs}`.

Run every command in the foreground. Never use background execution. Other workers may be editing `crates/impulcifer-dsp/src/*` (performance audit) at the same time; you do not touch that crate. Do not edit `features.toml`, `webview_ui/`, `i18n/`, or any other crate than the ones listed under Allowed files.

## Design rule
`impulcifer-service` owns everything that touches the file system and the job registry; `impulcifer-dsp` stays file-free. The 23-method IPC surface and its envelopes do not change (the frontend is 2.x `webview_ui` unchanged). Every user-visible string goes through the i18n catalogue keys the 2.x logger emits (`cli_*`), never a new hard-coded English string; when 2.x emits a key with arguments, emit the same key and arguments.

## Scope

### `crates/impulcifer-io/src/brir_layout.rs` (`core/brir_layout.py`)
- `pub fn append_track_names(path: &Path, names: &[&str]) -> Result<(), IoError>`: append the `ICHL` RIFF chunk carrying `{"version":1,"tracks":[...]}` exactly like lines 46 to 61 (chunk id, little-endian size, JSON bytes, pad byte on odd length, RIFF size patched).
- `pub fn read_track_names(path: &Path, canonical_order: &[&str], channel_count: usize) -> Result<Option<Vec<String>>, IoError>`: lines 63 to 100 (missing chunk returns `None`; invalid JSON, version, count mismatch or unknown names are `IoError::Format`).
- Tests: a round trip on a 4-track file, an odd-length JSON pad byte case, and a fixture read of a 2.x-written file (`export_goldens_service.py` writes one with `core.brir_layout.append_track_names`).

### `crates/impulcifer-service/src/brir/` (new module tree; `pub mod brir;` in `lib.rs`)
- `discovery.rs`: `pub struct MeasurementDir { pub dir: PathBuf, pub recordings: Vec<(PathBuf, Vec<String>)>, pub headphones: Option<PathBuf>, pub room: RoomDiscovery, pub eq: EqDiscovery, pub test_signal: Option<PathBuf> }` built by `pub fn discover(dir: &Path, config: &ProcessingConfig) -> Result<MeasurementDir, BrirError>` reproducing `open_binaural_measurements` (regex `^([A-Z]{2,3}(,[A-Z]{2,3})*)\.wav$` over a **sorted** listing so the order is deterministic across platforms; 2.x uses `os.listdir` order, document the difference and its harmlessness since the HRIR is keyed by speaker), `discover_room_measurements` (via `impulcifer_dsp::stages::room::discover_room_measurements` on the listing), `resolve_headphone_file` (via the dsp helper), the eq file names (`eq`, `eq-left`, `eq-right`, `.csv` before `.txt`), the `eq.wav` deprecation warning key, and the sidecar `test.wav`.
- `estimator.rs`: `pub fn open_estimator(dir: &MeasurementDir, test_signal: Option<&str>) -> Result<SweepEstimator, BrirError>` reproducing `open_impulse_response_estimator` (85 to 180): `TEST_SIGNALS` aliases resolved against the bundled `data/` directory (`paths::data_dir`), `generate:<duration>s@<fs>` with `snap_sweep_samples` (port the grid arithmetic from `core/sweep_detection.py`; put it in `crates/impulcifer-dsp`? No: it is small integer arithmetic, port it into `impulcifer-service::brir::sweep_grid` with a golden), `None|auto` (sidecar, then `detect_sweep_parameters` port from `core/sweep_detection.py` (M-grid snapping over the recordings' lengths; export a golden of its result on the demo directory), then the bundled default WAV through `SweepEstimator::from_samples`), `.wav` via `read_wav` + `from_samples`, `.mlp/.thd/.truehd` via `impulcifer_io::ffmpeg` to a temp WAV (`check_ffmpeg_available` semantics: `RuntimeError` becomes `BrirError::FfmpegMissing`).
- `inputs.rs`: `pub fn load_inputs(dir: &MeasurementDir, estimator: &SweepEstimator, config: &ProcessingConfig, events: &mut dyn BrirEvents) -> Result<PipelineInputs, BrirError>`: reads the recordings (`read_wav`, `open_recording_samples`), room measurements and the generic `room.wav`, `room-target.csv`/`room-mic-calibration.{csv,txt}` (`FrequencyResponse::read_from_csv` + `prepare_room_target`/`prepare_mic_calibration`), `headphones.wav` (+ writes `headphone-responses.wav`), `room-responses.wav`, the eq files (`read_eq_settings_csv`, `select_eq_pair`, `finalize_eq`; an EqualizerAPO config is reported as `BrirError::Unsupported` with the file name until P12), emitting the same `cli_*` info/warning keys in the same order as the 2.x stages (`cli_creating_estimator`, `cli_running_room_correction`, `cli_running_headphone_compensation`, `cli_creating_equalization`, `cli_creating_target`, `cli_opening_measurements`, the warnings `cli_warning_eq_wav_deprecated`, `cli_error_hp_file_missing`, `cli_warning_sweep_detect_fallback`, `cli_warning_test_signal_not_found`, `cli_mic_deviation_skipped_hpcomp`; read `core/pipeline.py` for the complete list and their arguments).
- `outputs.rs`: `pub fn write_outputs(dir: &Path, outputs: &PipelineOutputs, config: &ProcessingConfig, i18n: &Catalog, estimator: &SweepEstimator) -> Result<WrittenOutputs, BrirError>`: `responses.wav`, `README.md` (render `ReadmeData` as the 2.x markdown: the pipe table with the same column labels through the i18n keys `write_readme` uses, `%.2f` formats, the reflection section, applied gain and sample rate lines; compare byte-for-byte with a golden README generated by 2.x on the demo copy with the `en` catalogue and with `ko`), `hrir.wav`, `hesuvi.wav` (with `remove_silent_channels` → `compact_tracks` + `append_track_names`), `truehd_*`, `jamesdsp.wav`, `Hangloose/<SP>.wav`, all through `impulcifer_io::write_wav(.., 32)`; `WrittenOutputs { pub hesuvi: PathBuf, pub files: Vec<PathBuf> }`. Plot stages (`plot_pre`, `plot_post`, `plot_results`, `plot_additional`, `interactive_plots`) are not part of this packet: emit their progress steps when enabled (so the total matches 2.x) and log `cli_plots_not_available_yet` (add the key to your report; the parent adds it to the catalogue) instead of drawing.
- `run.rs`: `pub struct BrirRun { pub output_path: PathBuf }`, `pub fn run_brir(config: &ProcessingConfig, i18n: &Catalog, ctx: &JobContext) -> Result<BrirRun, JobFailure>`: `discover` → `open_estimator` → `load_inputs` → `impulcifer_dsp::pipeline::run_pipeline(config, inputs, &mut observer)` → `write_outputs`; the observer forwards `StageProgress` as job progress events with the 2.x payload shape `{"progress": step/total, "message": <translated cli key>, "key": <cli key>}` and `check_cancelled` through `JobContext::check_cancelled`; log lines as `{"level": "info"|"warning"|"error", "message": <translated>, "key": <key>}`; at the end the same `OUTPUT_MISSING` failure when `hesuvi.wav` is absent. `ErrorCode` mapping: `FILE_NOT_FOUND` for missing recordings/directory, `INVALID_REQUEST` for unsupported inputs, `INTERNAL_ERROR` otherwise, cancellation through `JobFailure { cancelled: true }`.
- `lib.rs`: implement `IpcMethod::StartBrir` (validation exists in `args.rs`/`ProcessingConfig::from_kwargs`-equivalent; reuse the 2.x `_validate_brir_request` rules already ported by P04 if present, otherwise port them: `dir_path` must be a directory, eq sidecar copies when `do_equalization`, the `decay` string to map parsing `FL:300,FR:300` and bare numbers in ms → seconds) and start the job with `JobRegistry::start(JobKind::Brir, true, ..)`; `PollJob`/`CancelJob` already exist. Also implement `IpcMethod::DetectSweep` (the `detect_sweep_parameters` port; response shape from `impulcifer_service.py:715-733`) and `IpcMethod::GenerateSweepSet` (`SweepEstimator::sweep_sequence` + `write_wav` + the `test.wav` sidecar and the file naming from `impulcifer-io::sweep_files`; response shape from the 2.x method) since they share the estimator code; leave `StartRecording`, `StartOutputRecovery`, the updater methods and `ListAudioDevices` as they are.

### Acceptance test: demo parity (M2 gate)
`crates/impulcifer-service/tests/demo_parity.rs`, `#[test] fn demo_brir_matches_python_within_budget` (and `#[ignore]`-free; it runs in CI on all three OSes): copy `data/demo` to a temp dir, run `run_brir` with the default config through a `JobRegistry`, then compare `hesuvi.wav` and `hrir.wav` against the 2.x outputs exported by `tests/migration/export_goldens_service.py` (which runs `python impulcifer.py --dir_path=<copy> --test_signal=data/sweep-6.15s-48000Hz-32bit-2.93Hz-24000Hz.wav` on `py -3.14` and stores per-track SHA-256, first/last 256 samples, max abs, RMS, peak index, and the full `FL-left`/`FL-right`/`FC-left` tracks as `.f64`): track names and count exact, sample rate exact, length exact, peak index exact, per-sample `atol 1e-3 * max_abs` on the full tracks and on the first/last 256 of every track (the EQ FIRs carry the minimum-phase oracle noise, see `tests/migration/README-fr.md`), max abs and RMS `rtol 1e-4`. Also the `--vbass --vbass_freq=250` scenario (the CI `brir-integrity` path) with the same budget, and `README.md` byte-equal for `en`. Record the wall-clock of the Rust run and of the Python run (the golden stores it) in the test output; the ratio is reported, not asserted here (that is `perf.pipeline-demo`).

Other tests: `crates/impulcifer-service/tests/brir_ipc.rs` (`start_brir_rejects_missing_dir`, `start_brir_emits_progress_with_cli_keys_and_total_eleven`, `start_brir_cancel_mid_run_reports_cancelled`, `start_brir_output_missing_when_write_fails` (read-only output dir), `detect_sweep_reports_demo_parameters`, `generate_sweep_set_writes_named_files_and_sidecar`), `crates/impulcifer-service/tests/sweep_grid.rs` (`snap_sweep_samples_matches_python` golden), `crates/impulcifer-io` tests for `brir_layout`.

Do not edit `features.toml`; report the test names for `ipc.start_brir`, `ipc.detect_sweep`, `ipc.generate_sweep_set`, the 26 `stage.*` keys (which are now exercised end to end), the `output.*` entries (`hrir_wav`, `hesuvi_wav`, `responses_wav`, `readme_txt`, `hangloose_wavs`, `jamesdsp_wav`, `truehd_layout_wavs`) and `output.recovery` (untouched).

## Allowed files
`E:/Impulcifer/crates/impulcifer-service/**` (except `settings.rs` and the existing IPC methods' behaviour), `E:/Impulcifer/crates/impulcifer-io/src/brir_layout.rs` and its `mod` line in `crates/impulcifer-io/src/lib.rs`, `E:/Impulcifer/crates/impulcifer-io/tests/brir_layout.rs`, `E:/Impulcifer/tests/migration/export_goldens_service.py`, `E:/Impulcifer/tests/migration/goldens/p11_*`, `E:/Impulcifer/tests/migration/README-service.md`. Nothing else.

## Hard rules
- `#![forbid(unsafe_code)]`; no unsafe. `impulcifer-service` may depend on `impulcifer-dsp`, `impulcifer-io`, `impulcifer-jobs`, `impulcifer-types` only (ARCHITECTURE section 2).
- The IPC envelopes, method names and argument shapes stay exactly as the 2.x frontend sends them (`webview_ui/app.js` is the consumer; read how it calls `start_brir`, `poll_job`, `detect_sweep`, `generate_sweep_set` and what it expects back).
- Every log or progress message uses a catalogue key that exists in `i18n/locales/en.json`; list any key you need that does not exist.
- `data/demo` in the repository stays untouched; every run uses a temporary copy.
- Reproduce 2.x file-writing order and names; do not add files 2.x does not write.

## Verification (foreground, paste output)
```
py -3.14 E:/Impulcifer/tests/migration/export_goldens_service.py
git -C E:/Impulcifer status --short data/demo
cargo fmt -p impulcifer-service -p impulcifer-io -- --check
cargo clippy -p impulcifer-service -p impulcifer-io --all-targets -- --no-deps -D warnings
cargo test -p impulcifer-service -p impulcifer-io
cargo test -p impulcifer-policy
```

## Report format
(1) files changed; (2) verification outputs verbatim including the demo parity numbers (max per-sample error per track, RMS/max-abs ratios, wall-clock Rust vs Python); (3) the event sequence of a default demo run (keys in order, total steps); (4) 2.x behaviours reproduced and any you could not; (5) test names per registry id; (6) catalogue keys you needed that do not exist; (7) anything undone. Do not end your turn before the commands complete.
